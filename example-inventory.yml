---
# Example inventory for Germanium detector IOC deployment
all:
  children:
    ioc_hosts:
      hosts:
        ioc-host-01:
          ansible_host: 192.168.1.50
          ansible_user: ansible
          ansible_become: true
          ansible_become_method: sudo
        ioc-host-02:
          ansible_host: 192.168.1.51
          ansible_user: ansible
          ansible_become: true
          ansible_become_method: sudo
      vars:
        # Common variables for all IOC hosts
        ansible_python_interpreter: /usr/bin/python3

